name: OV Build MacroQuest Emu RoF2 Beta

on:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  call_build:
    uses: ./.github/workflows/ov_build_release_shared.yaml
    with:
      client_target: beta-emu-rof2
    secrets: inherit
